import React, { Component } from 'react';

class Button extends Component {
    render(){
        return(
            <Button className="btn">
                <h2 className="btn-title">Sign In</h2>
            </Button>
        );
    }
}

export default Button